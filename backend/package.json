{
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "groq-sdk": "^1.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.6.2"
  },
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend API for CourseAI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "lint": "eslint . --ext js --report-unused-disable-directives --max-warnings 0",
    "check": "npm run lint && node --check server.js && node --check controllers/authController.js && node --check controllers/courseAiController.js && node --check controllers/courseController.js && node --check middlewares/auth0middleware.js && node --check utils/asyncQueue.js && node --check utils/rateLimiter.js",
    "test": "node --test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
